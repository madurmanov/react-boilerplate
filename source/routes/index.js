import { page as pageConstants } from '../constants'

const { PAGES } = pageConstants

const {
  HOME,
  ABOUT,
} = PAGES

export default {
  [HOME]: {
    path: '/',
  },
  [ABOUT]: {
    path: '/about',
  },
}
