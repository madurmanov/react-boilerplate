import * as actions from './actions'
import reducer from './reducer'
import container from './container'
import constants from './constants'
import selectors from './selectors'

export default {
  actions, reducer, container, constants, selectors,
}
